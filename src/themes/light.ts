import { Theme, ITheme } from 'common/types/theme';

const light: ITheme = {
  type: Theme.Light,
  palette: {
    background: '#FFFFFF',
    primary: '#366EFF',
    secondary: '#FFFFFF',
    text: '#212124',
  },
};

export default light;
