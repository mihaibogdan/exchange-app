import { Theme, ITheme } from 'typings/theme';

const dark: ITheme = {
  type: Theme.Dark,
  palette: {
    background: '#212124',
    primary: '#366EFF',
    secondary: '#FFFFFF',
    text: '#FFFFFF',
  },
};

export default dark;
